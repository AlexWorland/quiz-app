import { APIRequestContext } from '@playwright/test'
import { API_BASE_URL } from './api'

// Placeholder retained for future use; currently not invoked to avoid relying on backend reset endpoints.
export async function resetTestDatabase(_request: APIRequestContext) {
  return
}

